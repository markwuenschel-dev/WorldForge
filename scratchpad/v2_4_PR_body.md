v2.4 adds the first bounded tactical-behavior substrate for WorldForge. It defines tactical profiles, roles, affordances, NPC/group bindings, decision traces, state deltas, save/load state, budget reports, and OperatorForge tactical views; proves tactical decision/action/state evidence across the bounded streaming-region matrix; and hardens the system with negative validators, fuzz, torture, report integrity, runtime-truth guards, and regression gates.

## Runtime truth (read this first)

v2.4 proves bounded tactical behavior through **deterministic_tactical_simulation** over real WorldForge evidence. It is **not** live UE tactical AI, **not** BT/EQS execution, and **not** player-facing combat feel.

- No native navmesh claim.
- No GOAP/EQS/RL/LLM behavior.
- Combat remains v1.8 substrate.
- Streaming remains v2.3 substrate.
- Quest/faction remains v2.2 substrate.

## Scope

- Failure-code band **WF931–WF974** `TACTICAL_*` (WF975–1010 reserved).
- Matrix = 2 v2.3 regions × 3 roles (sentinel / skirmisher / suppressor) × 2 pressure profiles (baseline_tactical / high_pressure_tactical) × 2 seeds = **24 scenarios** (intentionally 24, not 120).
- Every tactical decision records its input, considered options, selected VALID option, execution result, and state mutation — no black-box AI. NPC behavior stays bounded by existing routes, anchors, cover affordances, and streaming scopes.

## Validation checklist — all GREEN under `--strict`

- tactical contracts GREEN (15 contracts dogfooded)
- tactical negative fixtures GREEN (51 known-bads, each owning-code)
- tactical profile generation + validation GREEN (3 roles, 2 profiles)
- tactical affordance generation + validation GREEN (24 maps; refs resolve on disk)
- tactical binding generation + validation GREEN (48 NPC bindings + 24 group states)
- tactical smoke GREEN
- 24/24 tactical runtime GREEN (24 runtime reports + 24 decision bundles)
- tactical save-load GREEN (24 states, genuine roundtrip)
- tactical budgets GREEN (24 reports, budget_result recomputed from raw values)
- operator tactical index + dashboard + smoke GREEN (24 scenario + 48 NPC views; all links resolve)
- tactical negative-validators GREEN (aggregate >60 known-bads)
- tactical fuzz-300 GREEN (0 accepted)
- tactical torture GREEN (24 modes, each caught for owning code)
- tactical report integrity GREEN (real TacticalEvidenceIndex + report-tree scan)
- tactical hygiene GREEN (artifact-count ledger)
- **v2.4 shield GREEN 23/23** — `python tools/pipeline/v2_4_shield.py --strict --tactical --advanced-ai`

### Regressions (shield-level) — all GREEN

- v2.3 shield `--streaming --worldscale` → GREEN 22/22
- v2.2 shield `--quests --factions` → GREEN 22/22
- v2.1 shield `--operator` → GREEN 20/20
- v2.0 shield `--package` → GREEN 4/4

v1.9 / v1.8 / v1.7 / v1.6z live-UE regressions were **not** re-run: they require `--require-live` engine runtime and are unaffected by v2.4, which is purely additive authoring/simulation over the v2.3 substrate (no v1.x combat/NPC/reward code touched).

## Honest caveats

- v2.4 adds a bounded tactical behavior substrate, **not** final game AI.
- The runtime matrix is intentionally 24 scenarios, not 120.
- `deterministic_tactical_simulation` is not live UE tactical AI.
- No behavior-tree editor, GOAP planner, EQS rewrite, RL, or LLM NPC behavior is included.
- OperatorForge views are local/static control-plane views; no player-facing AI UI.
- No new asset acquisition, Houdini live cook, World Partition, or multiplayer AI.

## Artifact hygiene

Commits contain source + generated artifacts + **deterministic** per-scenario evidence only. Intentionally **not** committed (regenerated by the gates each run; would churn): transient gate-summary reports (`validate_*`/`run_*`/`build_*_report.json`), the git-sha-stamped `tactical_evidence_index.json`, regression producer churn, and all UE transients (`Saved/`, `Intermediate/`, `DerivedDataCache/`, `Build/`). A clean checkout reproduces GREEN via the shield.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
